# AppPages

Reusable, static privacy and support page templates for portfolio apps. Start by copying the applicable template into `apps/<app-slug>/privacy/` or `support/`, then replace every bracketed placeholder only after checking the app's actual data practices.

The templates are deliberately not app-specific legal claims. Unknown facts must remain an explicit TODO and be resolved before publication.
